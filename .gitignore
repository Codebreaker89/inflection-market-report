# Credentials — never commit
config.py

# Local-only personal data
trades.csv

# Generated outputs (rebuilt on each run)
digest_preview.html
universe_cache.json
tracker.html
notify.log
# last_scan.json tracked intentionally — CI commits it for GitHub Pages + email matrix

# Python
__pycache__/
*.pyc
*.pyo
.env

# macOS
.DS_Store
